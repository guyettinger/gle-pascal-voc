/**
 * segmented type
 * This field signifies if the images contain annotations that are non-linear (irregular) in shape - commonly referred to as polygons.
 */
export enum Segmented {
    "Linear" = 0,
    "Non-Linear" = 1
}